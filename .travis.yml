language: csharp
dist: trusty
mono: none
dotnet: 2.1

branches:
  only:
  - master
  - xunit_dotnet_core

install:
- dotnet restore

script:
- dotnet build
- dotnet test dotnetcore/dotnetcore_test/dotnetcore_test.csproj

